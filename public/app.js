let DATA={menu:[],settings:{},reviews:[],coupons:[]},cart=[],heroIndex=0,heroTimer,couponDiscount=0,appliedCoupon='';let $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);function money(n){return (DATA.settings.currency||'₹')+Number(n).toLocaleString('en-IN')}
async function init(){const r=await fetch('/api/public-data');DATA=await r.json();renderHero();renderCats();renderMenu();renderReviews();startHero();}
function renderHero(){const s=DATA.settings.heroBanners||[];$('#heroSlides').innerHTML=s.map((x,i)=>`<div class="hero-slide ${i===0?'active':''}" style="background-image:url('${x.image}')"><div class="hero-content"><img class="hero-logo-white" src="/assets/logo-white.png" alt="WASABEE Oriental Cuisine"><span class="eyebrow">WASABEE • ORIENTAL CUISINE</span><h1>${x.title.replace('Oriental Excellence','Oriental <em>Excellence</em>')}</h1><p>${x.subtitle}</p><div class="hero-actions"><a class="btn purple" href="#order">Order Online →</a><a class="btn ghost" href="#booking">Book a Table</a></div></div></div>`).join('');$('#heroDots').innerHTML=s.map((_,i)=>`<span class="dot ${i===0?'active':''}"></span>`).join('')}
function startHero(){heroTimer=setInterval(heroNext,5000)}function heroNext(){const s=$$('.hero-slide');if(!s.length)return;heroIndex=(heroIndex+1)%s.length;heroPaint()}function heroPrev(){const s=$$('.hero-slide');if(!s.length)return;heroIndex=(heroIndex-1+s.length)%s.length;heroPaint()}function heroPaint(){$$('.hero-slide').forEach((x,i)=>x.classList.toggle('active',i===heroIndex));$$('.dot').forEach((x,i)=>x.classList.toggle('active',i===heroIndex))}
function renderCats(){const c=DATA.menu;$('#categories').innerHTML=c.map((x,i)=>`<button class="cat ${i===0?'active':''}" onclick="jumpCat('${x.id}')"><span class="icon">${x.icon||''}</span>${x.name}</button>`).join('')}
function flattenItems(cat){if(cat.items)return cat.items.map(x=>({...x,sub:''}));return (cat.subcategories||[]).flatMap(s=>(s.items||[]).map(x=>({...x,sub:s.name})));}
function imageFor(item){if(item.image)return item.image;const q=encodeURIComponent(item.name);return `https://source.unsplash.com/600x400/?${q},asian-food`}
function renderMenu(){let html='';for(const cat of DATA.menu){html+=`<section class="menu-category" id="cat-${cat.id}"><h3>${cat.icon||''} ${cat.name}</h3>`;if(cat.subcategories){for(const sub of cat.subcategories){html+=`<div class="subcat">${sub.name}</div><div class="grid">${sub.items.map(itemCard).join('')}</div>`}}else html+=`<div class="grid">${cat.items.map(itemCard).join('')}</div>`;html+='</section>'}$('#menu').innerHTML=html}
function itemCard(item){if(item.active===false)return '';const base=item.variants?Math.min(...item.variants.map(v=>v.price)):item.price;const label=item.variants?`${item.variants.length} choices`:money(base);return `<article class="food-card"><div class="food-img",url('${imageFor(item)}');background-size:cover;background-position:center"><span>${item.name.includes('Soup')?'🍲':item.name.toLowerCase().includes('sushi')?'':''}</span></div><div class="food-body"><h4>${item.name}</h4><p>${item.description||'Authentic oriental preparation crafted by WASABEE.'}</p>${item.variants?`<span class="variant-label">${label} • Select before adding</span>`:''}<div class="price-row"><span class="price">${item.variants?`From ${money(base)}`:money(item.price)}</span><button class="add" onclick='openItem(${JSON.stringify(item).replace(/'/g,"&#39;")})'>Add +</button></div></div></article>`}
function openItem(item){
 const groups=(item.addonGroups||[]).map(id=>(DATA.settings.addonGroups||[]).find(g=>g.id===id)).filter(g=>g&&g.active!==false);
 let addonHtml='';
 if(groups.length){addonHtml=`<div class="addon-section"><div class="addon-section-title"><span>➕ Customize Your Order</span><small>Choose options if you want</small></div>${groups.map(g=>`<div class="addon-group"><div class="addon-group-title"><div><h3>${g.name}</h3><small>${g.description||''}</small></div><span class="addon-rule">${g.required?'Required':'Optional'} · ${g.selection==='multiple'?'Choose multiple':'Choose one'}</span></div><div class="addon-choice-list">${(g.options||[]).filter(o=>o.active!==false).map(o=>`<button type="button" class="addon-choice" data-group="${g.id}" data-mode="${g.selection==='multiple'?'multiple':'single'}" data-price="${Number(o.price||0)}" data-name="${String(o.name).replace(/"/g,'&quot;')}" onclick="selectAddonChoice(this)"><span>${g.selection==='multiple'?'☐':'○'}</span><b>${o.name}</b><em>${Number(o.price||0)?'+'+money(o.price):'Included'}</em></button>`).join('')||'<div class="empty">No active options in this group.</div>'}</div></div>`).join('')}</div>`}
 const legacy=item.addons?.length?`<div class="addon-section"><div class="addon-section-title"><span>Choose Add-on</span><small>Legacy item options</small></div><div class="addon-list">${item.addons.map(a=>`<button class="addon" data-name="${String(a).replace(/"/g,'&quot;')}" data-price="0" onclick="this.classList.toggle('selected')">${a}</button>`).join('')}</div></div>`:'';
 $('#itemModalBody').innerHTML=`<span class="eyebrow">${item.variants?'CHOOSE VARIANT':'ADD TO CART'}</span><h2>${item.name}</h2><p>${item.description||'Authentic oriental preparation crafted by WASABEE.'}</p>${item.variants?`<div class="variant-list">${item.variants.map((v,i)=>`<button type="button" class="variant ${i===0?'selected':''}" data-price="${v.price}" onclick="selectVariant(this)"><span>${v.name}</span><b>${money(v.price)}</b></button>`).join('')}</div>`:`<input type="hidden" id="singlePrice" value="${item.price}">`}${addonHtml}${legacy}<div class="modal-price-note">Container charge is added at checkout.</div><div style="margin-top:22px"><button class="btn purple full" onclick='confirmAdd(${JSON.stringify(item).replace(/'/g,"&#39;")})'>Add to Cart</button></div>`;
 $('#itemModal').classList.add('show')
}
function selectVariant(el){$$('#itemModal .variant').forEach(x=>x.classList.remove('selected'));el.classList.add('selected')}
function selectAddonChoice(el){const mode=el.dataset.mode;if(mode==='single'){$$(`#itemModal .addon-choice[data-group="${CSS.escape(el.dataset.group)}"]`).forEach(x=>{x.classList.remove('selected');x.querySelector('span').textContent='○'});el.classList.add('selected');el.querySelector('span').textContent='●'}else{el.classList.toggle('selected');el.querySelector('span').textContent=el.classList.contains('selected')?'☑':'☐'}}
function closeItem(){$('#itemModal').classList.remove('show')}
function confirmAdd(item){let variant='';let price=Number(item.price||0);if(item.variants){const v=$('#itemModal .variant.selected');variant=v?.querySelector('span').textContent||item.variants[0].name;price=Number(v?.dataset.price||item.variants[0].price)}const groups=(item.addonGroups||[]).map(id=>(DATA.settings.addonGroups||[]).find(g=>g.id===id)).filter(g=>g&&g.active!==false);for(const g of groups){if(g.required&&!$$(`#itemModal .addon-choice[data-group="${CSS.escape(g.id)}"].selected`).length){toast(`Please choose ${g.name}`);return}}const addons=[...$$('#itemModal .addon-choice.selected')].map(x=>({name:x.dataset.name,price:Number(x.dataset.price||0)}));const legacy=[...$$('#itemModal .addon.selected')].map(x=>({name:x.dataset.name||x.textContent.trim(),price:Number(x.dataset.price||0)}));addons.push(...legacy);const addonTotal=addons.reduce((s,a)=>s+Number(a.price||0),0);cart.push({key:Date.now()+Math.random(),id:item.id,name:item.name,variant,addons,price:price+addonTotal,basePrice:price,qty:1,containerCharge:Number(item.containerCharge??DATA.settings.defaultContainerCharge??0)});closeItem();renderCart();toast('Added to cart ✓')}
function renderCart(){const count=cart.reduce((s,x)=>s+x.qty,0);$('#cartCount').textContent=count;$('#drawerCount').textContent=count;$('#cartItems').innerHTML=cart.length?cart.map((x,i)=>`<div class="cart-row"><div class="cart-thumb">🍜</div><div class="cart-info"><h4>${x.name}</h4><small>${x.variant||''}${x.addons?.length?' · '+x.addons.map(a=>typeof a==='string'?a:a.name).join(', '):''}</small><div class="qty"><button onclick="changeQty(${i},-1)">−</button><b>${x.qty}</b><button onclick="changeQty(${i},1)">+</button></div></div><div><b>${money(x.price*x.qty)}</b><button class="remove" onclick="removeItem(${i})">🗑</button></div></div>`).join(''):'<div class="empty">Your cart is empty.<br>Add something delicious!</div>';const sub=cart.reduce((s,x)=>s+x.price*x.qty,0),discount=sub*(Number(DATA.settings.discountPercent||15)/100),gst=(sub-discount)*(Number(DATA.settings.gstPercent||5)/100),total=sub-discount+gst;$('#cartSummary').innerHTML=`<div class="sum"><span>Sub Total</span><b>${money(sub)}</b></div><div class="sum"><span>Discount (${DATA.settings.discountPercent}%)</span><b style="color:#14863b">−${money(discount)}</b></div><div class="sum"><span>GST (${DATA.settings.gstPercent}%)</span><b>${money(gst)}</b></div><div class="sum total"><span>Items Total</span><b style="color:var(--purple)">${money(total)}</b></div><small style="color:#857686">Container charges are shown at checkout.</small>`}
function changeQty(i,d){cart[i].qty+=d;if(cart[i].qty<=0)cart.splice(i,1);renderCart()}function removeItem(i){cart.splice(i,1);renderCart()}function openCart(){$('#cartDrawer').classList.add('open');$('#overlay').classList.add('show')}function closeCart(){$('#cartDrawer').classList.remove('open');$('#overlay').classList.remove('show')}function goCheckout(){if(!cart.length)return toast('Please add items first');closeCart();renderCheckout();$('#checkoutModal').classList.add('show')}function closeCheckout(){$('#checkoutModal').classList.remove('show')}
function renderCheckout(){const sub=cart.reduce((s,x)=>s+x.price*x.qty,0),baseDiscount=sub*(Number(DATA.settings.discountPercent||15)/100),gst=(sub-baseDiscount-couponDiscount)*(Number(DATA.settings.gstPercent||5)/100),container=cart.reduce((s,x)=>s+(Number(x.containerCharge??DATA.settings.defaultContainerCharge??0)*x.qty),0),total=sub-baseDiscount-couponDiscount+gst+container;$('#checkoutTotal').innerHTML=`<div class="sum"><span>Subtotal</span><b>${money(sub)}</b></div><div class="sum"><span>${DATA.settings.discountPercent}% Discount</span><b>−${money(baseDiscount)}</b></div><div class="sum"><span>Coupon ${appliedCoupon?'('+appliedCoupon+')':''}</span><b>${couponDiscount?'−'+money(couponDiscount):money(0)}</b></div><div class="sum"><span>GST (${DATA.settings.gstPercent}%)</span><b>${money(gst)}</b></div><div class="sum"><span>Container</span><b>${money(container)}</b></div><div class="sum total"><span>Payable</span><b style="color:var(--purple)">${money(total)}</b></div>`}
let customerLocation='';function applyCoupon(){const code=($('#couponCode')?.value||'').trim().toUpperCase();const sub=cart.reduce((s,x)=>s+x.price*x.qty,0);const c=(DATA.coupons||[]).find(x=>x.active!==false&&String(x.code).toUpperCase()===code);if(!c){couponDiscount=0;appliedCoupon='';$('#couponStatus').textContent=code?'Invalid coupon code.':'';$('#couponStatus').className='coupon-bad';renderCheckout();return}if(sub<Number(c.minOrder||0)){couponDiscount=0;appliedCoupon='';$('#couponStatus').textContent='Minimum order for this coupon is '+money(c.minOrder);$('#couponStatus').className='coupon-bad';renderCheckout();return}couponDiscount=c.type==='flat'?Math.min(sub,Number(c.value||0)):sub*Number(c.value||0)/100;appliedCoupon=c.code;$('#couponStatus').textContent='Coupon applied ✓';$('#couponStatus').className='coupon-ok';renderCheckout()}function getLocation(){if(!navigator.geolocation)return $('#locationStatus').textContent='Geolocation not supported';$('#locationStatus').textContent='Getting location…';navigator.geolocation.getCurrentPosition(p=>{customerLocation=`https://www.google.com/maps?q=${p.coords.latitude},${p.coords.longitude}`;$('#locationStatus').textContent='Current location added ✓';},()=>$('#locationStatus').textContent='Could not get location. Please enter address manually.')}
$('#checkoutForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target);const sub=cart.reduce((s,x)=>s+x.price*x.qty,0),discount=sub*DATA.settings.discountPercent/100,gst=(sub-discount-couponDiscount)*DATA.settings.gstPercent/100,container=cart.reduce((s,x)=>s+(Number(x.containerCharge??DATA.settings.defaultContainerCharge??0)*x.qty),0),total=sub-discount-couponDiscount+gst+container;const customer={name:f.get('name'),phone:f.get('phone'),address:f.get('address'),landmark:f.get('landmark'),location:customerLocation};const order={customer,items:cart,subtotal:sub,discount,gst,containerCharge:container,total};const r=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(order)});const data=await r.json();let text=`*WASABEE NEW ONLINE ORDER*\nOrder: ${data.orderId}\n\n*Customer*\nName: ${customer.name}\nPhone: ${customer.phone}\nAddress: ${customer.address}\nLandmark: ${customer.landmark||'-'}\nLocation: ${customerLocation||'-'}\n\n*Items*\n`;cart.forEach(x=>text+=`• ${x.name}${x.variant?' ('+x.variant+')':''}${x.addons?.length?' ['+x.addons.map(a=>typeof a==='string'?a:a.name).join(', ')+']':''} × ${x.qty} = ${money(x.price*x.qty)}\n`);text+=`\nSubtotal: ${money(sub)}\nDiscount: -${money(discount)}\nCoupon ${appliedCoupon||''}: -${money(couponDiscount)}\nGST: ${money(gst)}\nContainer: ${money(container)}\n*TOTAL: ${money(total)}*`;window.open(`https://wa.me/${DATA.settings.whatsappNumber}?text=${encodeURIComponent(text)}`,'_blank');cart=[];couponDiscount=0;appliedCoupon='';renderCart();closeCheckout();toast('Order sent to WhatsApp ✓')});
$('#bookingForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target);const b=Object.fromEntries(f.entries());await fetch('/api/bookings',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(b)});const text=`*WASABEE TABLE BOOKING REQUEST*\nName: ${b.name}\nPhone: ${b.phone}\nDate: ${b.date}\nTime: ${b.time}\nGuests: ${b.guests}\nNotes: ${b.notes||'-'}`;window.open(`https://wa.me/${DATA.settings.whatsappNumber}?text=${encodeURIComponent(text)}`,'_blank');e.target.reset();toast('Booking request sent ✓')});
function renderReviews(){const rs=DATA.reviews||[];$('#reviewsList').innerHTML=rs.length?rs.map(r=>`<div class="review"><div class="stars">${'★'.repeat(Number(r.rating||5))}${'☆'.repeat(5-Number(r.rating||5))}</div><h4>${r.name}</h4><p>${r.comment}</p><small>${new Date(r.createdAt).toLocaleDateString()}</small></div>`).join(''):'<div class="review"><div class="stars">★★★★★</div><h4>Be our first reviewer</h4><p>Share your WASABEE experience with us.</p></div>';$('#reviewSummary').innerHTML=`<div class="stars">★★★★★</div><b>${rs.length? 'Loved by our guests':'Your review matters'}</b>`}
$('#reviewForm').addEventListener('submit',async e=>{e.preventDefault();const f=new FormData(e.target);await fetch('/api/reviews',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:f.get('name'),comment:f.get('comment'),rating:window.reviewRating||5})});e.target.reset();toast('Thank you for your review ✓');const r=await fetch('/api/public-data');DATA=await r.json();renderReviews()});$('#starsInput').addEventListener('click',e=>{if(e.target.closest('#starsInput')){window.reviewRating=5;$('#starsInput').style.color='#d09b00'}});function jumpCat(id){document.querySelector('#cat-'+id)?.scrollIntoView({behavior:'smooth',block:'start'});$$('.cat').forEach(x=>x.classList.remove('active'));event?.currentTarget?.classList.add('active')}function toggleNav(){$('.topbar nav').classList.toggle('show')}function toast(msg){const t=$('#toast');t.className='toast show';t.textContent=msg;setTimeout(()=>t.className='toast',2400)}init();
